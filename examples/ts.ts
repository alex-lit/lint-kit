export interface Test {
  a: 'a';

  type1: number;

  type2: number;
  /**
   * Test dsaf
   *
   * Asfas
   */
  type3: number;
}

export const qw = 1;

/** Test */
export const q = 1;
export const { b, c, x } = { b: 1, c: 1, x: 1 };

export enum Enumerator {
  a = 1,

  b = 2,

  c = 3,
  d = 4,

  er = 5,
}
